Resolved. Cause: Floor 3 DHCP scope Option 006 still referenced decommissioned DNS servers after migration, which prevented domain controller resolution and caused login/Group Policy failure on three Win11 endpoints. Action: Updated Floor 3 DHCP DNS settings to active resolver(s) with 10.10.0.10 primary, renewed affected client leases, refreshed DNS state, and validated DC discovery and Group Policy processing success. Preventive: Enforce mandatory DHCP Option 006/015 pre/post migration validation per subnet, add stale DNS scope monitoring, and run per-floor canary checks for DC resolution, SYSVOL access, and gpupdate success. User confirmed working.
